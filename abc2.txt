
git commit \newline

git commit \newline

git commit \newline

git commit \newline

git checkout -b bugFix \newline

git commit \newline

git commit \newline

git checkout master \newline

git commit \newline

git commit \newline

git commit \newline

git commit \newline

git commit \newline

git checkout bugFix \newline

git merge master\wedge\wedge \newline

git commit \newline

git commit \newline

git checkout HEAD\wedge\wedge\wedge\wedge\wedge\wedge\wedge \newline

git checkout -b branch1 \newline

git commit \newline

git commit \newline

git commit \newline

git commit \newline

git commit \newline

git checkout HEAD\wedge\wedge\wedge\wedge\wedge\wedge \newline

git checkout -b branch2 \newline

git commit \newline

git commit \newline

git commit \newline

git merge branch1 \newline

git checkout master \newline

git branch -d branch1 \newline

git branch -d branch2 \newline
